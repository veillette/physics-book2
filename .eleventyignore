# Eleventy input exclusions (we run with setUseGitIgnore(false), so .gitignore no
# longer drives these). During migration dir.input is src/, so most of these are
# outside the input tree already; they matter once input flips to the repo root at
# cutover (P10). node_modules is always ignored by Eleventy regardless.
_site
_site_jekyll_baseline
vendor
README.md
CONTRIBUTE.md
CHANGELOG.md
claude.md
roadmap.md
LICENSE.txt
doc
scripts
tests
hooks
